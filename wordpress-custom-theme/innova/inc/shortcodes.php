<?php
    /**
    *   Shortcodes
    *
    *   @package Crunch
    *   @since Crunch 2.0.0
    */

    /* ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ */
    /* ~~~~~~~~~~ Shortcodes ~~~~~~~~~~ */
    /* ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ */

        /* ~~~~~~~~~~ Button ~~~~~~~~~~ */

        // function button_func( $atts, $content = "" ) {
        //     $return = '<a href="'.$atts['url'].'" class="co-exist-button';

        //     if($atts['type'] == 'full-background') {
        //         $return .= ' co-exist-button__full-background co-exist-button__full-background--primary-color';
        //     } elseif($atts['type'] == 'text-only') {
        //         $return .= ' co-exist-button__text-only co-exist-button__text-only--primary-color';
        //     }

        //     $return .= '">'.$atts['text'].'</a>';

        //     return $return;
        // }
        // add_shortcode( 'button', 'button_func' );


        // /* ~~~~~~~~~~ Blockquote ~~~~~~~~~~ */

        // function blockquote_func( $atts, $content = "" ) {
        //     $return = '<blockquote class="blockquote">';
        //         $return .= '<div class="blockquote__content content">' .apply_filters('content', $content). '</div>';
        //         $return .= '<footer class="blockquote__footer">';
        //             $return .= '<cite class="blockquote__footer__cite d-block element-small-margin-top">~ '.$atts['cite'].'</cite>';
        //         $return .= '</footer>';
        //     $return .= '</blockquote>';

        //     return $return;
        // }
        // add_shortcode( 'blockquote', 'blockquote_func' );


        // /* ~~~~~~~~~~ Panel ~~~~~~~~~~ */

        // function panel_func( $atts, $content = "" ) {
        //     $return = '<div class="panel';
        //         if($atts['type']=='full-background') {
        //             $return .= ' panel--full-background';
        //         } elseif($atts['type']=='outline') {
        //             $return .= ' panel--outline';
        //         }
        //     $return .= '">';
        //         $return .= apply_filters('content', $content);
        //     $return .= '</div>';

        //     return $return;
        // }
        // add_shortcode( 'panel', 'panel_func' );

    ?>